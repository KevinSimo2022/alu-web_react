// Define `RowID` as a type alias for `number`
export type RowID = number;

// Define `RowElement` interface to represent a row structure
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number;
}
